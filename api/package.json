{
  "name": "portfolio-api",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild contact.ts --bundle --platform=node --target=node22 --outfile=dist/contact.js",
    "dev": "node --env-file=.env --watch --experimental-strip-types dev.ts"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
