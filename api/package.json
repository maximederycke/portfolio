{
  "name": "portfolio-api",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild contact.ts --bundle --platform=node --target=node22 --format=esm --outfile=dist/contact.js",
    "dev": "node --env-file=.env --watch --experimental-strip-types dev.ts"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.28.0"
  }
}
